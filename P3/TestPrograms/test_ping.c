#include <stdio.h>

/**
 * This program prints a single, predictable line
 * to STANDARD OUTPUT.
 * Used to test file redirection (>) and piping (|).
 */
int main() {
    printf("Hello from the pinger!\n");
    return 0;
}